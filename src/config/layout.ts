/**
 * Layout-related constants for non-CSS contexts (e.g. TS math, docs).
 * Prefer Tailwind tokens (`px-gutter-x`, `py-section-y-*`) in components.
 */

/** Mirrors `--container-max-width` (see `src/styles/tokens/container.css`). */
export const CONTAINER_MAX_WIDTH_REM = 80.625;
